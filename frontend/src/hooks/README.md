# Custom React Hooks Folder (`src/hooks/`)

This folder contains global reusable React hooks:

* **`useDebounce.ts`**: Debounces fast state updates (e.g., search query updates).
* **`useAuth.ts`**: Quick wrapper around Redux store auth states.
* **`useResponsive.ts`**: Custom media-query listeners binding to MUI breakpoints.
