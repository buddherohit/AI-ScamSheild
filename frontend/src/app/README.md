# Application Initializer Folder (`src/app/`)

This folder manages root level React components:

* **`App.tsx`**: Bootstraps all context providers (Redux Provider, MUI ThemeProvider, CSS baseline, and React Router).
* **`main.tsx`**: The main entry point rendering the React tree inside the DOM mount target.
