# TypeScript Types Folder (`src/types/`)

This directory holds global type definitions, interfaces, and namespaces:

* **`api.ts`**: Standard API success/error structure definitions matching the Spring Boot payload envelopes.
* **`models.ts`**: Standard model interfaces (User, ScamReport, NotificationLog).
* **`index.d.ts`**: Custom global ambient types.
