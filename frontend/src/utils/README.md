# Pure Utility Functions Folder (`src/utils/`)

This folder contains pure utility functions that contain zero React or state bindings:

* **`dateFormatter.ts`**: Unified date-time formatting functions.
* **`jwtHelper.ts`**: Decodes payload fields from access tokens.
* **`formatters.ts`**: Currency or phone formatters.
