# Static Assets Folder (`src/assets/`)

This directory houses static assets that will be compiled into the Vite build pipeline:

* **`images/`**: Brand backgrounds, landing layouts, illustrations.
* **`svg/`**: Vector icons, brand shields, status banners.
