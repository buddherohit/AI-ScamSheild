# Structural Layouts Folder (`src/layouts/`)

This folder manages structural layout shells:

* **`AuthLayout.tsx`**: Guest layouts wrapping auth cards (login, registration).
* **`MainLayout.tsx`**: Authenticated layout containing the header, theme triggers, responsive side navigation, user menu, and global notification toasts.
