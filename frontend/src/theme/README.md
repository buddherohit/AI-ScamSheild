# Theme Configuration Folder (`src/theme/`)

This directory establishes the application style system:

* **`theme.ts`**: The main configuration file exporting the Material UI `lightTheme` and `darkTheme` settings.
* **Tokens**: Incorporates curated, premium typography details (Outfit, Inter) and HSL color definitions.
