# Application Constants Folder (`src/constants/`)

This directory houses static configuration variables and schemas:

* **`apiEndpoints.ts`**: Collection of backend REST paths.
* **`regexPatterns.ts`**: Shared validation patterns (e.g. Phone, email, UPI strings).
* **`appConfig.ts`**: Static configuration bounds (session timeouts, pagination defaults).
